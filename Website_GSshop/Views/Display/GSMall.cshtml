@using Website_GSshop.Models
@model List<GSMall>
<div class="container-fluid">
    <h2 class="article-list__heading"><img src="~/Content/Layout/images/icons8-fire-48.png" width="40" /><a href="#">GSMall</a></h2>
</div>
<div class="container-fluid">
    <br />
    <center>
        <div class="btn-22">
            <a href="@Url.Action("AllMostPopular","Ajax")" data-ajax="true" data-ajax-method="GET" data-ajax-update="#change" data-ajax-mode="replace"><span class="span"><b>XEM TẤT CẢ</b></span></a>
        </div>
    </center>
    <br />
    <br />
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-2">
                <div class="card hovercard">
                    <div class="cardheader" style="background-image: url('../../Content/Layout/images/@item.gsmall_bg'); ">
                    </div>
                    <div class="avatar1">
                        <img src="~/Content/Layout/images/@item.gsmall_image" alt="" />
                    </div>
                    <div class="info">
                        <div class="title">
                            <a href="@Url.Action("GSMallDetail","View", new {@id = item.product_id})" style="color: black">@item.gsmall_name</a>
                        </div>
                        <a href="@Url.Action("GSMallDetail","View", new {@id = item.product_id})"><small class="desc" style="color: #a6a6a6">@item.gsmall_slogan</small></a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
<br />
<br />
